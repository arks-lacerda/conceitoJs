// Operador de String (String operator)

// comparison
console.log( "a" == "a")

// concatenation
// retorna a união de duas String
/*
let alpha ='alpha'
console.log(alpha + "bet" + "s")
*/

let alpha ='alpha'
alpha += "bet"
console.log(alpha + 345) // transforma número em uma string